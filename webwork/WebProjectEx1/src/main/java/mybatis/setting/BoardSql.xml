<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="smart">
	<select id="getTotalCount" resultType="int">
		select count(*) from smart
	</select>
	<insert id="insertSmart" parameterType="sdto">
		insert into smart (writer, subject, content) values(#{writer}, #{subject}, #{content})
	</insert>
	<select id="selectOfSmart" resultType="sdto">
		select * from smart order by num desc
	</select>
	<update id="updateReadCount" parameterType="int">
		update smart set readcount=readcount+1 where num=#{num}
	</update>
	<update id="updateLikes" parameterType="int">
		update smart set likes = likes+1 where num=#{num}
	</update>
	<select id="selectOneOfSmart" parameterType="int" resultType="sdto">
		select * from smart where num = #{num}
	</select>
	<delete id="deleteOfSmart" parameterType="int">
		delete from smart where num = #{num}
	</delete>
	<update id="updateOfSmart" parameterType="sdto">
		update smart set writer = #{writer }, subject = #{subject }, content = #{content } where num = #{num } 
	</update>
</mapper>